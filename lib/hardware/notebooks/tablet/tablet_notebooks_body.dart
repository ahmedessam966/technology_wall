import 'package:flutter/material.dart';

class TabletNotebooksBody extends StatelessWidget {
  const TabletNotebooksBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
