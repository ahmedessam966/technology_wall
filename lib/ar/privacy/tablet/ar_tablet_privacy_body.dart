import 'package:flutter/material.dart';

class ARTabletPrivacyBody extends StatelessWidget {
  const ARTabletPrivacyBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
