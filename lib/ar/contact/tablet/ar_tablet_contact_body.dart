import 'package:flutter/material.dart';

class ARTabletContactBody extends StatelessWidget {
  const ARTabletContactBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
