import 'package:flutter/material.dart';

class TabletPartsBody extends StatelessWidget {
  const TabletPartsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
