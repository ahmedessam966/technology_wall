import 'package:flutter/material.dart';

class WebPartsBody extends StatelessWidget {
  const WebPartsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
