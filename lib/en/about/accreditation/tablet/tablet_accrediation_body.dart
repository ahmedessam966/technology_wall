import 'package:flutter/material.dart';

class TabletAccreditationBody extends StatelessWidget {
  const TabletAccreditationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
