import 'package:flutter/material.dart';

class TabletAccountPageBody extends StatelessWidget {
  const TabletAccountPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
