part of '../web/web_sap_body.dart';

class SAPSupplyChain extends StatelessWidget {
  const SAPSupplyChain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
