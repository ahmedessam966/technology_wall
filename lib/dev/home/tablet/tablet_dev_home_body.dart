import 'package:flutter/material.dart';

class TabletDevHomeBody extends StatelessWidget {
  const TabletDevHomeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
