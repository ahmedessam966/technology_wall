import 'package:flutter/material.dart';

class TabletContactBody extends StatelessWidget {
  const TabletContactBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
