import 'package:flutter/material.dart';

class TabletInventoryBody extends StatelessWidget {
  const TabletInventoryBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
