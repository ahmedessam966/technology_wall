import 'package:flutter/material.dart';

class MobilePrivacyBody extends StatelessWidget {
  const MobilePrivacyBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
